.page-header
  %h1
    Import

.row
  %h2 Import Stories from CSV file
  - form_tag project_import_path, :method => :post, :multipart => true do
    %label(for='file') File to Upload:
    != file_field_tag 'file'
    != submit_tag 'Import'